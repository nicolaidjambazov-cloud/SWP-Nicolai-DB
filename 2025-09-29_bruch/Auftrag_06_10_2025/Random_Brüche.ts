const zeahler=Math.ceil(Math.random()*100);
const nenner=Math.ceil(Math.random()*100)
let zeahler2=Math.ceil(Math.random()*100);
let ausgangszähler;
if(zeahler>zeahler2)
{
    ausgangszähler=zeahler-zeahler2
}










